---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: awsvpcendpointservices.ec2operator.cloud-infra-group.io.cloud-infra-group.io
spec:
  group: ec2operator.cloud-infra-group.io.cloud-infra-group.io
  names:
    kind: AWSVPCEndpointService
    listKind: AWSVPCEndpointServiceList
    plural: awsvpcendpointservices
    singular: awsvpcendpointservice
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AWSVPCEndpointService is the Schema for the awsvpcendpointservices
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AWSVPCEndpointServiceSpec defines the desired state of AWSVPCEndpointService.
            properties:
              acceptanceRequired:
                description: AcceptanceRequired indicates whether endpoint connections
                  require acceptance
                type: boolean
              networkLoadBalancerARN:
                description: |-
                  NetworkLoadBalancerARN specifies the ARN of the NLB used for the endpoint service
                  This field is immutable after the resource is created.
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              tags:
                additionalProperties:
                  type: string
                description: |-
                  Tags are optional metadata tags to apply to the VPC endpoint service.
                  Keys must match the regex pattern `^([\p{L}\p{Z}\p{N}_.:/=+\-@]*)$`, have a minimum length of 1,
                  and a maximum length of 128. Values must match the same regex pattern and have a maximum length of 256.
                maxProperties: 50
                type: object
            required:
            - acceptanceRequired
            - networkLoadBalancerARN
            type: object
          status:
            description: AWSVPCEndpointServiceStatus defines the observed state of
              AWSVPCEndpointService.
            properties:
              acceptedPrincipals:
                description: AcceptedPrincipals is a list of AWS principal ARNs that
                  currently have permissions on the service
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              errorMessage:
                description: ErrorMessage provides details if there was an error during
                  reconciliation
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the controller successfully
                  reconciled this resource
                format: date-time
                type: string
              serviceID:
                description: ServiceID is the unique identifier of the VPC Endpoint
                  Service in AWS
                type: string
              state:
                description: State represents the current state of the VPC Endpoint
                  Service (e.g., Available, Pending, Failed)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
